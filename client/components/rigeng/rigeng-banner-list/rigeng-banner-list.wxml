<view>
  <book-title
    title="{{ bookData.title }}"
    book-id="{{ bookData.book_id }}"
    needSwitch="{{ false }}"
    show-more="{{ false }}"
  ></book-title>
  <banner-item
    wx:for="{{ bannerList }}"
    wx:key="{{ item.commic_id }}"
    comic-item="{{ item }}"
  ></banner-item>
</view>
